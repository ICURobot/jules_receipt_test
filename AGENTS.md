Project Setup
This is a Flutter project. The Flutter SDK is manually installed at /tmp/flutter.

Dev Environment Instructions
Before running any flutter commands, always run: export PATH="$PATH:/tmp/flutter/bin"
